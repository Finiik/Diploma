/* ============================================
   theme — public API
   ============================================ */

export { ThemeProvider, useTheme } from './context/ThemeContext';
export type { Theme } from './services/theme';
