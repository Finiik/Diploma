/* ============================================
   search — public API
   ============================================ */

export { search, rebuildIndex } from './services/search';
export { default as SearchBar } from './components/SearchBar/SearchBar';
